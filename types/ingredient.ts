export type IngredientItem = {
  id: string;
  name: string;
  emoji: string;
  bgColor: string;
};

export type CookingIngredientItem = {
  emoji: string;
  name: string;
  qty: string;
  bg: string;
};
 
